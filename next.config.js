/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        
        domains: ['picsum.photos', '123picsum.photos', 'cdn.midjourney.com', 'fastly.picsum.photos', 'chat.openai.com', 'www.google.com'],
            
          
    },
}

module.exports = nextConfig
